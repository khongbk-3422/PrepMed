# --- PostgreSQL Credentials ---
DB_USER=prepmed
DB_PASSWORD=predmed12345
DB_NAME=prepmed
DB_HOST=postgres-db
DB_PORT=5432

# --- Core API Database Connection String ---
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --- Cloud Foundation Keys ---
# 👇 Your active Gemini Key is safely tucked away here, out of source control
GEMINI_API_KEY=Google_API_Key

# --- External Services Bridges ---
OLLAMA_URL=http://docker.internal
